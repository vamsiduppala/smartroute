# Normalize line endings to LF in the repository regardless of a contributor's local
# core.autocrlf -- this repo is built/tested by a Linux CI runner, and mixed line endings
# on Windows checkouts were producing a "LF will be replaced by CRLF" warning on every commit.
* text=auto eol=lf

*.jar binary
*.png binary
*.jpg binary
