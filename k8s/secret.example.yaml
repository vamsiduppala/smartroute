# Template only — do NOT commit a real key. Copy to secret.yaml (gitignored) and fill in,
# or create imperatively:
#   kubectl create secret generic smartroute-secrets --from-literal=openai-api-key=sk-...
apiVersion: v1
kind: Secret
metadata:
  name: smartroute-secrets
type: Opaque
stringData:
  openai-api-key: "REPLACE_ME"
